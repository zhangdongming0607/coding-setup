# 第 3 课作业

本目录提交第 3 课的 Issue 看板作业。

完成内容：

- 在 `IssueCard` 里新增 `priority` Props，支持 `P0 / P1 / P2`
- 在卡片上显示优先级
- 切换状态时保留原有 `priority`
- 整理 Demo 项目里的所有 `useState`

## 运行

```bash
npm install
npm run dev
```

如果本机 `npm` 有异常，也可以用：

```bash
corepack pnpm install
corepack pnpm dev
```

## 说明文件

- `useState-notes.md`：整理本项目里所有 `useState`
